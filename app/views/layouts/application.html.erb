<!DOCTYPE html>
<html>
<head>
  <title>Scoring</title>
  
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= stylesheet_link_tag    "bootstrap", media: "screen", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  
  <div id="wrap">
    <!-- Static navbar -->
    <nav class="navbar navbar-default navbar-static-top" role="navigation">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="<%= root_path %>">Scoring</a>
        </div>
        <div class="navbar-collapse collapse">
          <ul class="nav navbar-nav">
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">Games <span class="badge"><%= games_count %></span> <b class="caret"></b></a>
              <ul class="dropdown-menu">
                <li><%= link_to "All", games_path %></li>
                <li class="divider"></li>
                <% @games.each do |game| %>
                  <li><%= link_to game.title, game %></li>
                <% end %>
              </ul>
            </li>
          </ul>
          <ul class="nav navbar-nav navbar-right">
            <li><a href="<%= new_game_path %>"><span class="glyphicon glyphicon-plus"></span> new game</a></li>
          </ul>
        </div><!--/.nav-collapse -->
      </div><!-- /.container -->
    </nav>
    
    <div class="container">
      <%= yield %>
    </div>
  
  </div>

  <footer id="footer">
    <div class="container">
      <p class="text-muted credit">
        Scoring app version <%= Scoring::VERSION %>
        by <a href="http://jonathangeorges.fr">JoJoS</a>
      </p>
    </div>
  </footer>
  
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "bootstrap", "data-turbolinks-track" => true %>
</body>
</html>
